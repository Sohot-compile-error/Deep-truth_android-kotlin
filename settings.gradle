pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url "https://repo.spring.io/libs-release"
        }
        maven {
            url "https://github.com/tanersener/maven-repo-ffmeg-android/raw/master"
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "compileerror"
include ':app'
